import React, { Component } from "react";

export default function WaitRoom({ socket }) {
  return (
    <div>
      <h1 className="text-white">Waiting for all players to join ...</h1>
    </div>
  );
}
